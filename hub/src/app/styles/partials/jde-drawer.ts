import {primaryClr} from './colors/colors';
import {DrawerDesignTokens} from '@primeuix/themes/types/drawer';

export const jdeDrawer: DrawerDesignTokens = {
  root: {
    background: primaryClr[600]
  }
}
