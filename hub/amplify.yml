version: 1
applications:
  - appRoot: hub
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build -- --configuration production
      artifacts:
        baseDirectory: dist/hub
        files:
          - '**/*'
      runtime:
        nodejs: 20.19
